connector.name=iceberg
# Loại catalog: Dùng Hive Metastore (Chuẩn Enterprise)
iceberg.catalog.type=hive_metastore

# Địa chỉ của con server vừa dựng ở bước 1
hive.metastore.uri=thrift://hive-metastore:9083

# --- CẤU HÌNH MINIO (S3) ---
# Trino dùng "fs.native-s3" chứ không phải "hive.s3"
fs.native-s3.enabled=true
s3.endpoint=http://minio:9000
s3.region=us-east-1
s3.path-style-access=true
s3.aws-access-key=minio_admin
s3.aws-secret-key=minio_password
#s3.ssl.enabled=false

# Quan trọng: Cho phép Trino ghi đè file (nếu cần)
iceberg.file-format=PARQUET
iceberg.register-table-procedure.enabled=true